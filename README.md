# Flow Architecture UI — Features routes

This workspace contains a minimal Next.js app with a `/features` entry that links to `ADMIN` and `USER` feature pages.

Run locally:

```bash
npm install
npm run dev
```

Visit: http://localhost:3000 — you'll be redirected to `/features`.
